const { categoriesRouter } = require("./categories.router");
const { productsRouter } = require("./products.router");
const { cartRouter } = require("./cart.routes");

module.exports = { categoriesRouter, productsRouter, cartRouter };
